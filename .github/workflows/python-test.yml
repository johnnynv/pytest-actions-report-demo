name: Python CI

on: [push, pull_request]

# 最小权限声明（不依赖仓库全局设置）
permissions:
  checks: write  # 仅用于更新检查状态
  contents: read # 默认已有，显式声明更安全

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Run tests
      run: |
        pip install pytest pytest-html
        pytest --html=report.html --self-contained-html

    - name: Upload HTML Report
      uses: actions/upload-artifact@v4
      with:
        name: pytest-report
        path: report.html

    - name: Show Summary
      run: |
        echo "### 📊 Pytest Results" >> $GITHUB_STEP_SUMMARY
        echo "- 🔍 [Download Full Report](https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID)" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Passed: $(grep -o 'passed.*' report.html | cut -d'<' -f1)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Failed: $(grep -o 'failed.*' report.html | cut -d'<' -f1)" >> $GITHUB_STEP_SUMMARY

